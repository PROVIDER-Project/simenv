"""Qt application layer: graphics items, map view, controls, entrypoint.

Depends only on the DataSource seam (visualization.data.source) and the static
layout config (visualization.config.layout) — never on pandas/CSV directly.
"""
